name: CD

on:
  push:
    branches: master

jobs:
  website:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name:  Install JupyterBook
      run: pip install jupyter-book
      shell: bash -l {0}

    - name: Make book
      run: jupyter-book-0.3 create site --content-folder docs

    - name: Build book
      run: jupyter-book-0.3 build site

    - name: Deploy
      if: success() && github.event_name == 'push' && github.ref == 'refs/heads/master'
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_branch: gh-pages
        publish_dir: site
        force_orphan: true
        enable_jekyll: true
        cname: github.com
